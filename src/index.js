const activo = false;
// si se cumple o no cumple
const mensaje = (activo) ? 'activo':'inactivo';

//Si solo se cumple
const mensajeAct = (activo) && 'activo se cumple';
console.log(mensaje);
console.log(mensajeAct);
